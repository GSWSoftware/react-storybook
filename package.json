{
  "name": "@support-monorepo/storybook",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deps:upgrade": "ncu -u",
    "prestart": "build-storybook -s public",
    "start": "start-storybook -p 9009 -s public"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "react-scripts": "^4.0.1"
  }
}
